# Orbital-Attitude-Control-Agent
An Agentic System to dynamically balance spacecraft orbital attitude given user instructions via RL and Lang-graph

## Installation Instructions

### Install Docker on your machine (Mac Instuctions)

````bash
brew install --cask docker
#Then launch Docker Desktop application
````
### Install requirements
```bash
pip install -r requirements.txt
```


